using UnityEngine;
using System.Collections;

public class EnemyBulletSpawner : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Invoke ("testPool", 2f);	
	}

	void testPool(){
		GameObject go = EnemyBulletPooler._Instance.getBullet (0);
		//Debug.Log (go);
	}

	// Update is called once per frame
	void Update () {
	
	}
}
